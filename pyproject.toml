[project]
name = "iruna-relic"
version = "0.1.0"
description = "Iruna relic 7x7 puzzle solver"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "opencv-python",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
